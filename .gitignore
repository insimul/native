# CMake build tree (contains the fetched Trealla source under _deps/).
build/
build-*/

# Packaging output (US-LI5).
dist/

# Cargo build tree for the Rust bindings (US-FFI1).
rust/target/

# The measurement's intermediate records (tasklist 250 US-3). The published
# figures are bench/results/measurements.json, which IS committed; the per-run
# JSON-Lines dumps and corpus logs under raw/ are regenerated by
# scripts/measure.sh and are not evidence anyone reads twice.
bench/results/raw/
